# Exact hero reference implementation — 2026-09-10

Status: `IMPLEMENTED_ON_BRANCH__PR_CI_PENDING`

The user explicitly froze the supplied hero screenshot as the visual target and requested implementation without creative reinterpretation.

## Active implementation

- `hero-validated-scene.webp` is an optimized derivative of the user-approved photographic alpine binder scene.
- `index.html` uses the photographic asset directly rather than recreating the binders/environment with CSS or SVG placeholders.
- `hero-refined.css` matches the approved composition: branded topbar, large black serif headline, two CTAs, three value blocks, photographic archive scene and lower listening/events/contracts/narratives strip.
- `hero-interactions.js` overlays restrained pointer parallax and transparent navigation hotspots. The image itself remains visually unchanged.
- `prefers-reduced-motion` removes the parallax.
- `/docs.html` remains the premium Judge Packet.

## Unchanged product surfaces

The investigation workspace, four canonical cases, real Shannon proof rail and live read-only Shannon readback remain unchanged below the hero.

## Authority / truth boundary

No blockchain write, wallet connection, signature path, Packet 003 private identifier publication, engine semantic change or protected submission action is introduced by this visual change.
